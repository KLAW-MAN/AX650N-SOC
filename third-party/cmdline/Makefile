CUR_PATH			:= $(shell pwd)
HOME_PATH			:= $(abspath $(CUR_PATH)/../..)
ROOTFS_TARGET_PATH	?= $(HOME_PATH)/rootfs/rootfs/usr/local

include $(HOME_PATH)/build/color.mk
include $(HOME_PATH)/build/config.mak

all:
	@echo -e $(GREEN)"\nBuild cmdline success!!\n"  $(DONE)

install:
	@echo -e $(GREEN)"\nInstall cmdline success!!\n"  $(DONE)

clean:
	@echo -e $(GREEN)"\nClean cmdline success!!\n"  $(DONE)
