Usage Instructions:

1. Relevant Commands:
    • make clean -- Cleans only (removes build artifacts).
    • make -- Compiles FFmpeg with cleaning.
    • make DBG=yes -- Compiles FFmpeg with debugging information included in the binaries and shared objects (.so files).

2. The binaries and shared objects generated by the above compilation commands are located in the install directory.
3. Make compiles the version run on ARM64 architecture.
