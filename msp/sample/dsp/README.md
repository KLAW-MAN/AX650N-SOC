1. This test is used to verify the function of the VDSP module;

2. Operation method:
Place the BIN (itcm.bin and sram.bin) required by DSP in the same directory as sample_dsp, and then execute the following command:

#sample_dsp itcm.bin sram.bin

3. Normal operation log output displays PASS, indicating that the DSP is running normally.
