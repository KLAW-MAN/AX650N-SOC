if TARGET_AX650_SLAVE

config SYS_BOARD
	default "ax650_slave"

config SYS_VENDOR
	default "axera"

config SYS_CONFIG_NAME
	default "ax650_slave"

config BOARD_SPECIFIC_OPTIONS # dummy
	def_bool y

config BOOT_OPTIMIZATION
	def_bool y
	help
	  Enable boot optimization.
	  1. New Image and dtb image format.

endif
